# Copy to .env.local and fill in. Blank = localStorage-only (single device).
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_POOL_ID=default
# Optional live scores/stats proxy (workers/live). Blank = final scores only.
NEXT_PUBLIC_LIVE_API_URL=
